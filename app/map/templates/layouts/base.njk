{% from "nhsuk/components/breadcrumb/macro.njk" import breadcrumb %}
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ pageTitle }} | Journey map prototype</title>
    <meta name="description" content="{{ description }}">
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="{{ assetRoot }}/map.css">
    <script src="/nhsuk-frontend/nhsuk-frontend.min.js" type="module"></script>
    <script type="module">
      import { initAll } from '/nhsuk-frontend/nhsuk-frontend.min.js'
      initAll()
    </script>
    <script src="{{ assetRoot }}/map.js" defer></script>
    {% block headAssets %}{% endblock %}
  </head>
  <body class="map-site {{ bodyClass }} js-enabled nhsuk-frontend-supported">
    <div class="map-shell">
      {% block chrome %}
        {% if breadcrumbParams %}
          {{ breadcrumb(breadcrumbParams) }}
        {% endif %}
      {% endblock %}
      <main class="map-main" id="maincontent">
        {% block content %}{% endblock %}
      </main>
    </div>
    {% block bodyScripts %}{% endblock %}
  </body>
</html>
